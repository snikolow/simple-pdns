{% embed 'base.html.twig' %}
    {% set pageTitle = 'form_titles.domains' | trans({}, 'titles') %}

    {% block body %}
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    {{ form(form) }}
                </div>
            </div>
        </div>
    </div>
    {% endblock %}
{% endembed %}
